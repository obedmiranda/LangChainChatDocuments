# PDF Chat 🧠💬

Upload your documents and chat with them using LangChain, PGVector, and FastAPI.

## Run locally
```bash
./run.sh
```

## Folder structure
- app/core → config, db, logger
- app/services → embeddings, RAG
- app/utils → pdf loader
